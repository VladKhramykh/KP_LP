STANDART;

glavn
{
init num first;
init str message;
init num second;
message = "Привет мамукас";

first = strlen(message);
write(first);

back 0;
}
num function sum(num a,num b,num c)
{
init num result;
result = (a+b)*c*b;
write (result);
back result;
}
